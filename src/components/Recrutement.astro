<section class="recrutement">
	<div class="wrap">
		<h2>Envie de rejoindre l&rsquo;aventure&nbsp;?</h2>
		<p>
			Petit ou grand, débutant ou confirmé&nbsp;: viens essayer le handball
			islois. Le premier entraînement d&rsquo;essai est gratuit.
		</p>
		<a class="cta" href="/rejoindre">Essai gratuit →</a>
	</div>
</section>

<style>
	.recrutement {
		background: var(--color-crimson);
		padding: 3.5rem 1.5rem;
		text-align: center;
	}
	.wrap {
		max-width: 560px;
		margin: 0 auto;
	}
	h2 {
		color: var(--color-white);
		font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
		font-weight: 800;
		font-size: clamp(1.6rem, 4vw, 2.2rem);
		margin: 0 0 1rem;
	}
	p {
		color: var(--color-white);
		font-size: 1.05rem;
		line-height: 1.6;
		margin: 0 0 1.75rem;
		opacity: 0.95;
	}
	.cta {
		display: inline-block;
		background: var(--color-white);
		color: var(--color-crimson);
		font-weight: 800;
		font-size: 1.05rem;
		text-decoration: none;
		padding: 0.9rem 2rem;
		border-radius: 999px;
		transition: transform 0.15s ease;
	}
	.cta:hover {
		transform: translateY(-2px);
	}
</style>
